#include<stdio.h>
#include<math.h>
int main()
{
    //Instrution and Operators
    /*
    //valid Declaration
    int a = 22;
    int b = a;
    int c = b * 6;
    int d = 1, e;

    //Declaring multiple variable in one 
    int a, b, c;
    a=b=c=1;

    // invalid Declaration 
    int oldAge = 22;
    int newAge = oldAge + years; // useing befor declaration is invadli
    int years = 2; */

    /*
    Arithmetic Instructions

    a = b + c 
    a = b * c
    a = b / c
    int b, c;

   b = c = 1;

   int a = b + c;
   int power = pow(b,c);
   printf("%d", power);

   * Modular Operartor (%)

   retuen reminder for int 

   3 % 2 = 1

   -3 % 2 = -1
   example: 
   printf("%d", 16%10);


 //Type Conversion 

 int op int --------> int
 int op float ---------> float
 float op float ---------> float

  int a = (int) 1.999999;
   printf("%d \n", a);

   // Operator Precedence
   *, /, %     x = 4 + 9 * 10

   +, -


   =           x = 4 * 3 / 6 * 2



   // Associativity (For same precedence)

   left to right

   x = 4 * 3 / 6 * 2 

   int a = 4 * 3 / 6 * 2;
  printf("%d \n", a);

   int a = 5 * 2 - 2 * 3;
  printf("%d \n", a);

   int a = 5 * 2 / 2 * 3;
  printf("%d \n", a);

   int a = 5*(2/2)*3;
  printf("%d \n", a);

   int a = 5+2 / 2 * 3;
  printf("%d \n", a);


    */
  
  /*
  --> Control Instruction
  Used to determine flow of program

  a. Sequence Control
  b. decision Control
  c. loop Control
  d. Case Control

    a. Sequence Control
 ---> All above codes are the Sequence Control because the flow of program is sequence wise.
 
    

  
  */


   
   


    return 0;
}